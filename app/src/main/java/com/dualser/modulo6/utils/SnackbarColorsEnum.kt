package com.dualser.modulo6.utils

import com.dualser.modulo6.R

enum class SnackbarColorsEnum(val color: Int) {
    ACCEPT(R.color.accepted),
    OK(R.color.ok),
    WARN(R.color.warning),
    DANGER(R.color.danger),
}